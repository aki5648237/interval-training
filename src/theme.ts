import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
	typography: {
		// fontFamily: "YuGothic、游ゴシック体",
		// fontSize: 20,

		h1: { fontSize: 32, fontFamily: "MS ゴシック", fontWeight: "530"},
		h2: { fontSize: 23},
		h3: { fontSize: 20}
	},
});
export default theme; 