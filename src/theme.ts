import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
	typography: {
		fontSize: 15,

		h1: { fontSize: 32},
		h2: { fontSize: 24},
		h3: { fontSize: 20}
	},
	
});
export default theme; 